name: Release artifacts

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  validate-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85.0
      - name: Require the tag to match the workspace version
        run: |
          version="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')"
          test "${GITHUB_REF_NAME}" = "v${version}"

  build:
    needs: validate-tag
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            artifact: aarch64-unknown-linux-gnu
          - os: macos-13
            artifact: x86_64-apple-darwin
          - os: macos-14
            artifact: aarch64-apple-darwin
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85.0
      - name: Build the exact tagged revision
        env:
          ZSHCTL_BUILD_IDENTITY: ${{ github.sha }}
        run: cargo build --locked --release --bins
      - name: Package
        run: |
          mkdir package
          printf '%s\n' '${{ github.sha }}' > package/BUILD_IDENTITY
          cp target/release/zshctl target/release/zshctld zshctl.zsh LICENSE README.md package/
          cp -R shells docs spec scripts package/
          tar -czf "zshctl-${{ github.ref_name }}-${{ matrix.artifact }}.tar.gz" -C package .
          shasum -a 256 "zshctl-${{ github.ref_name }}-${{ matrix.artifact }}.tar.gz" > "zshctl-${{ github.ref_name }}-${{ matrix.artifact }}.tar.gz.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: zshctl-*.tar.gz*

  verify:
    needs: validate-tag
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85.0
      - run: |
          sudo apt-get update && sudo apt-get install -y jq zsh fzf
          go install github.com/x-motemen/ghq@v1.9.4
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - run: cargo build --locked --release --bins
      - run: ZSHCTL_BIN="$PWD/target/release/zshctl" tests/daemon/lifecycle.sh
      - run: ZSHCTL_BIN="$PWD/target/release/zshctl" tests/history/stress.sh
      - run: ZSHCTL_BIN="$PWD/target/release/zshctl" tests/config/cache.sh
      - run: ZSHCTL_BIN="$PWD/target/release/zshctl" tests/zsh/fallback.sh
      - run: ZSHCTL_BIN="$PWD/target/release/zshctl" tests/features/completion.sh
      - run: ZSHCTL_BIN="$PWD/target/release/zshctl" tests/release/matrix.sh

  benchmark:
    needs: validate-tag
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85.0
      - run: brew install hyperfine
      - run: cargo build --locked --release --bins
      - run: ZSHCTL_BIN="$PWD/target/release/zshctl" scripts/benchmark.sh
      - uses: actions/upload-artifact@v4
        with:
          name: performance-results
          path: |
            spec/benchmark-results/*
            spec/performance-budgets.json
            spec/performance-results.md

  publish:
    needs: [build, verify, benchmark]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
