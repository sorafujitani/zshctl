#autoload
emulate -L zsh
local -a out
out=("${(f)$(command zshctl --mode=next-placeholder --input.lbuffer="$LBUFFER" --input.rbuffer="$RBUFFER")}")
if [[ $out[1] == success ]]; then
  BUFFER=$out[2]
  CURSOR=$out[3]
fi
zle reset-prompt
