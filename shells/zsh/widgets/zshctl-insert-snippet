#autoload
emulate -L zsh
autoload -Uz zshctl-snippet-picker
local picker_status
zshctl-snippet-picker insert >/dev/null
picker_status=$?
if (( picker_status == 2 )); then
  zle "${ZSHCTL_COMPLETION_FALLBACK:-expand-or-complete}"
else
  zle reset-prompt
fi
return 0
